# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database

# Authentication & Security
JWT_SECRET=Xg1YXcC7f7R0INh+RjZuodPe0uki90wHleHz/rNmIYQ=
OWNER_EMAIL=admin@tacmap.com.au  # Admin user email

# Resend (Magic Link Email Service)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
MAGIC_LINK_FROM_EMAIL=noreply@tacmap.com.au

# Application URLs
APP_URL=https://tacmap.com.au  # Your production URL (used in magic link emails)
VITE_APP_ID=your-app-id  # Optional: App identifier

# Redis (Optional - for caching)
REDIS_URL=redis://default:password@host:port

# External APIs (Optional)
WAZE_API_KEY=your-waze-api-key-if-needed

# Map Providers (Client-side)
VITE_RECONMAP_DEFAULT_PROVIDER=mapbox
VITE_MAPTILER_API_KEY=your-maptiler-key
VITE_MAPTILER_STYLE=019ba6b7-5a01-7042-bc9a-d1ace6393958
VITE_MAPTILER_STYLE=streets-v2
VITE_MAPBOX_ACCESS_TOKEN=your-mapbox-access-token
VITE_MAPBOX_STYLE_ID=mapbox/streets-v12

# Legacy OAuth (Optional - can be removed if using magic links only)
OAUTH_SERVER_URL=https://oauth.example.com
OWNER_OPEN_ID=legacy-admin-open-id

# Built-in Services (Optional)
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# Environment
NODE_ENV=development  # or 'production'
PORT=3001

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit the actual env.local file with real credentials to version control
# 2. Rotate all credentials immediately if they are ever exposed
# 3. Use strong, random values for JWT_SECRET (minimum 32 characters)
# 4. Set OWNER_EMAIL to your actual admin email address
# 5. Configure CORS in vercel.json to match your APP_URL domain
# 6. Enable HTTPS in production (secure cookies require it)
